#include <stdio.h>
#include "PIE.h"

int main() {
    printf("Enter the radius of your circle: \n");
    int circleRad;
    scanf("%d", &circleRad);
    circalc(circleRad);
    circu(circleRad);
    return 0;
}